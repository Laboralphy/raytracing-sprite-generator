#!/bin/bash

render() {
	local sInput="$1"
	local fClock="$2"
	local sOutput="$3"
	if [ -z "$sOutput" ]
	then
		sOutput="${sInput}_$fClock"
	fi
	mkdir -p output
	povray -I$sInput.pov -W512 -H384 +UA +Q9 +A0.4 +K$fClock +P -Ooutput/$sOutput.png
}

render $*
