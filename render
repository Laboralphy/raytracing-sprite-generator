#!/bin/bash

render() {
	sInput="$1"
	sOutput="$2"
	fClock="$3"
	if [ -z "$sOutput" ]
	then
		sOutput="$sInput"
	fi
	mkdir -p output
	povray -I$sInput.pov -W1024 -H768 +UA +Q9 +A0.4 +K$fClock -Ooutput/$sOutput.png
}

render $*
